# แผนดีไซน์ — พัสดุไทย.com

เอกสารนี้เป็นข้อตกลงเรื่องหน้าตาของเว็บ เขียนก่อนลงมือเขียนโค้ด
แก้ที่นี่ก่อนเสมอเมื่อจะเปลี่ยนทิศทางดีไซน์

---

## 0. ข้อมูลอ้างอิงจาก track123.com

ดึง CSS จริงจาก `track123.com/_nuxt/*.css` (3 ไฟล์ รวม ~344 KB) แล้วนับความถี่ของสี

| สี | บทบาทในเว็บเขา | หมายเหตุ |
|---|---|---|
| `#5606AE` | **สีแบรนด์จริง** — หัวข้อ hero, พื้นปุ่มหลัก, เส้นขอบช่องกรอก | ม่วงเข้ม |
| `#7127BE` | ม่วงอ่อนกว่า ใช้เป็น state รอง | |
| `#FAF9FB` | พื้นหลังอ่อนอมม่วง | |
| `#E3DBEB`, `#CDC5D6` | เส้นขอบอมม่วง | |
| `#0E0C11` | ตัวอักษรเกือบดำ | |
| `#409EFF`, `#C0C4CC`, `#F56C6C` | — | **ไม่ใช่สีแบรนด์** เป็นค่า default ของ Element UI (`.el-button`) ที่ติดมากับ framework |

**ข้อสรุปที่ได้**: สมมติฐานตั้งต้นว่า track123 เป็นโทนน้ำเงินนั้นไม่ตรงกับของจริง — เขาเป็นม่วง
สิ่งที่หยิบมาใช้จึงไม่ใช่ค่าสี แต่เป็น **โครงสร้างของระบบสี**: สีแบรนด์เข้มหนึ่งตัวที่ใช้ทั้งหัวข้อและปุ่ม,
พื้นหลังอ่อนที่อมสีแบรนด์ (ไม่ใช่เทากลางๆ), และเส้นขอบที่อมสีแบรนด์เช่นกัน

---

## 1. Color

ทิศทาง: **"เอกสารไปรษณีย์"** — กระดาษซองจดหมายสีอุ่น + หมึกน้ำเงินเข้ม + ตราประทับสีชาด
ไม่ใช่ขาว-เทาเย็นแบบ SaaS ทั่วไป และไม่ใช่ม่วงแบบ track123

| Token | Hex | ใช้ที่ไหน |
|---|---|---|
| `--color-ink` | `#16325C` | สีหลัก — หัวข้อ, พื้นปุ่มหลัก, จุดล่าสุดใน timeline |
| `--color-ink-strong` | `#0E2340` | สถานะ hover/active ของปุ่ม, ตัวอักษรที่ต้องเน้นสุด |
| `--color-seal` | `#C8402C` | **สีชาด** — ใช้เฉพาะตราประทับ (signature element) กับสถานะที่มีปัญหา |
| `--color-paper` | `#F6F3EC` | พื้นหลังหลักของหน้า — โทนกระดาษซองอุ่น |
| `--color-line` | `#E2DCD0` | เส้นคั่นเชิงตกแต่ง (เส้น timeline, เส้นแบ่งส่วน) |
| `--color-line-strong` | `#8C806E` | ขอบขององค์ประกอบที่กรอก/กดได้ ต้องผ่าน 3:1 ตาม WCAG 1.4.11 |
| `--color-body` | `#1C1917` | ตัวอักษรเนื้อหา |
| `--color-faint` | `#5F594F` | ข้อความรอง เวลา สถานที่ |

สีบอกสถานะ (ใช้เท่าที่จำเป็น ไม่ให้แย่งความสนใจ):

| Token | Hex | ความหมาย |
|---|---|---|
| `--color-ok` | `#2F6F4E` | นำจ่ายสำเร็จ |
| `--color-alert` | `#C8402C` | มีปัญหา (ใช้ `--color-seal` ตัวเดียวกัน) |

**เหตุผลที่เลือกกระดาษอุ่นแทนขาว-เทาเย็น**: หน้าติดตามพัสดุคือ "การอ่านเอกสาร" ไม่ใช่ dashboard
พื้นอุ่นทำให้การ์ดสีขาวลอยขึ้นมาโดยไม่ต้องใช้เงาหนัก และแยกตัวจากเว็บ tracking อื่นได้ทันทีตั้งแต่วินาทีแรก

Contrast ที่วัดจริงแล้ว (คำนวณตามสูตร WCAG):

| คู่สี | อัตราส่วน | เกณฑ์ |
|---|---|---|
| ขาวบนหมึกน้ำเงิน (ปุ่มหลัก) | 12.77:1 | ผ่าน AA |
| `--color-body` บนกระดาษ | 15.78:1 | ผ่าน AA |
| หมึกน้ำเงินบนกระดาษ (หัวข้อ) | 11.52:1 | ผ่าน AA |
| `--color-faint` บนกระดาษ | 6.26:1 | ผ่าน AA |
| ชาดบนการ์ดขาว (ตราประทับ) | 4.97:1 | ผ่าน AA |
| เขียวบนการ์ดขาว (สถานะสำเร็จ) | 5.99:1 | ผ่าน AA |
| `--color-line-strong` บนกระดาษ | 3.49:1 | ผ่าน 1.4.11 (ขอบ UI) |

หมายเหตุจากการวัด: `--color-line` อยู่ที่ 1.23:1 กับพื้นกระดาษ ซึ่งบางเกินไปสำหรับ**ขอบช่องกรอก**
จึงต้องแยก `--color-line-strong` ออกมาใช้กับช่องกรอกโดยเฉพาะ ส่วน `--color-line` เหลือไว้ใช้กับ
เส้นคั่นเชิงตกแต่งที่ไม่ได้สื่อขอบเขตของสิ่งที่กดได้ (ซึ่งไม่อยู่ใต้เกณฑ์ 3:1)

---

## 2. Type

ไม่ใช้ `font-sans` default ของ Tailwind เพราะ fallback ภาษาไทยบน Windows จะกลายเป็น Tahoma
ซึ่งความสูง x-height กับระยะบรรทัดไม่เข้ากับ Latin

| บทบาท | ฟอนต์ | เหตุผล |
|---|---|---|
| หัวข้อ | **Anuphan** (400/500/600/700) | ฟอนต์ไทยที่ออกแบบโดย Cadson Demak รูปทรงเรขาคณิต หนักแน่น มีบุคลิกชัดตอนขนาดใหญ่ ไม่ใช่ Noto ที่เจอทุกเว็บ |
| เนื้อหา | **IBM Plex Sans Thai** (400/500/600) | ออกแบบมาเพื่ออ่านขนาดเล็กโดยเฉพาะ ช่องไฟกว้าง หัวอักษรชัด อ่านง่ายบนมือถือ และมี Latin คู่กันที่เข้าชุดกันจริง |

โหลดผ่าน `next/font/google` (self-host อัตโนมัติ ไม่มี request ไป Google ตอน runtime)
subset: `thai` + `latin`

ขนาดบนมือถือ: หัวข้อ hero 28px, เนื้อหา 16px, ข้อความรอง 14px — ไม่ต่ำกว่า 14px ทุกจุด

---

## 3. Layout

**Hero หนึ่งประโยค**: ช่องกรอกเลขพัสดุคือพระเอกกลางจอ วางบนพื้นกระดาษอุ่น
มีหัวข้อสั้นๆ อยู่เหนือมัน และรายชื่อขนส่งที่รองรับอยู่ใต้มัน ไม่มีอะไรอื่นมาแย่งพื้นที่

```
มือถือ (< 640px)                    จอใหญ่ (≥ 640px)
┌─────────────────────────┐         ┌──────────────────────────────────────┐
│ [◈] พัสดุไทย.com   [ผู้ใช้]│         │ [◈] พัสดุไทย.com          [เข้าสู่ระบบ] │
├─────────────────────────┤         ├──────────────────────────────────────┤
│                         │         │                                      │
│   พัสดุถึงไหนแล้ว        │         │        พัสดุถึงไหนแล้ว                │
│   เช็คได้ทุกขนส่ง         │         │        เช็คได้ทุกขนส่ง                 │
│                         │         │                                      │
│  ┌───────────────────┐  │         │   ┌────────────────────┬──────────┐  │
│  │ เช่น EE000000000TH│  │         │   │ เช่น EE000000000TH │ ค้นหา    │  │
│  └───────────────────┘  │         │   └────────────────────┴──────────┘  │
│  ┌───────────────────┐  │         │                                      │
│  │   ค้นหาพัสดุ       │  │         │   ไปรษณีย์ไทย · Flash · Kerry · J&T   │
│  └───────────────────┘  │         │                                      │
│                         │         └──────────────────────────────────────┘
│  ไปรษณีย์ไทย · Flash ·   │
│  Kerry · J&T · SPX      │          ผลลัพธ์: การ์ดกว้างสุด 640px จัดกลาง
├─────────────────────────┤
│  ╭───────────────────╮  │         ┌──────────────────────────────────────┐
│  │    ╭─────╮        │  │         │  ╭─────╮  EE000000000TH              │
│  │    │ ตรา │ ขนส่ง   │  │         │  │ ตรา │  ไปรษณีย์ไทย                 │
│  │    │ประทับ│ เลขพัสดุ│  │         │  ╰─────╯  อยู่ระหว่างขนส่ง            │
│  │    ╰─────╯        │  │         ├──────────────────────────────────────┤
│  ├───────────────────┤  │         │  ● ส่งออกจากศูนย์ฯ    3 ส.ค. 11:41    │
│  │ ● เหตุการณ์ล่าสุด  │  │         │  │                                   │
│  │ │                 │  │         │  ○ นำจ่ายสำเร็จ       26 พ.ค. 10:22   │
│  │ ○ เหตุการณ์ก่อนหน้า│  │         └──────────────────────────────────────┘
│  ╰───────────────────╯  │
└─────────────────────────┘
```

Mobile-first: ทุก class เขียนสำหรับจอแคบก่อน แล้วค่อยเติม `sm:` / `md:`
ปุ่มและช่องกรอกสูง 56px บนมือถือ (เกินเกณฑ์นิ้วสัมผัส 44px ของ Apple/Google)

---

## 4. Signature element — ตราประทับไปรษณีย์

**สิ่งที่ทำให้จำหน้านี้ได้: ตราประทับ (postmark) วงกลมที่มีเส้นคลื่นแบบตราลบแสตมป์**

ใช้ motif เดียวนี้ในสองจังหวะที่ต่างกัน:

1. **ตอนกำลังค้นหา** — วงแหวนตราประทับปรากฏขึ้นและหมุนช้าๆ พร้อมเส้นคลื่นด้านใน
   แทนที่จะเป็น spinner กลมๆ แบบที่เจอได้ทุกเว็บ
2. **ตอนได้ผลลัพธ์** — ตราประทับ "ประทับลง" (scale 1.5 → 1 พร้อมหมุนเล็กน้อยและ fade)
   ค้างอยู่ที่หัวการ์ด โดย**ข้างในวงคือวันที่กับเวลาที่อัปเดตล่าสุด** เหมือนตราประทับวันที่
   บนซองจดหมายจริง ส่วน**ข้อความสถานะวางอยู่ข้างๆ ตัวใหญ่** เพราะข้อความสถานะภาษาไทย
   ยาวเกินกว่าจะใส่ในวงกลมได้ (เช่น "ส่งออกจากที่ทำการแลกเปลี่ยนระหว่างประเทศขาออก")

   บนจอแคบกว่า 640px ตราประทับย้ายไปอยู่บรรทัดบนแทนที่จะอยู่ข้างซ้าย
   เพื่อให้ข้อความสถานะยาวๆ ได้ความกว้างเต็มการ์ด

สีของตราคือ `--color-seal` (ชาด) ซึ่งเป็นสีเดียวในหน้าที่ไม่ใช่น้ำเงิน/กระดาษ —
จึงดึงสายตาไปที่ "สถานะตอนนี้" ซึ่งเป็นข้อมูลที่ผู้ใช้มาหาจริงๆ

**ทำไมถึงเหมาะกับเว็บพัสดุไทย**: ตราประทับเป็นสัญลักษณ์ที่คนไทยทุกวัยเชื่อมโยงกับไปรษณีย์ได้ทันที
โดยไม่ต้องอธิบาย และมันสื่อ "ยืนยันแล้ว ณ เวลานี้" ตรงกับสิ่งที่หน้านี้ทำจริงคือรายงานสถานะ ณ เวลาหนึ่ง
ต่างจากไอคอนกล่องพัสดุหรือรถบรรทุกที่เว็บ tracking ใช้กันจนกลายเป็นภาพจำรวมๆ ที่แยกไม่ออก

**ส่วนที่เหลือของหน้าต้องเงียบ**: ไม่มี gradient, ไม่มีเงาหนา, ไม่มีไอคอนสีสด,
timeline เป็นเส้นตรงกับจุดกลมธรรมดา ให้ตราประทับเป็นจุดเดียวที่กล้า

### โลโก้ใน header

**ซองจดหมายที่ถูกประทับตราแล้วที่มุมขวาล่าง** — inline SVG วาดเอง viewBox 32×32 เส้นหนา 2
ใช้ `currentColor` จึงรับสีจาก `--color-ink` ตามระบบ token

วงตราใช้ `fill: var(--color-paper)` เพื่อกลบเส้นซองที่อยู่ข้างใต้ ให้อ่านเป็นสองชั้นจริง —
**ข้อผูกมัด: โลโก้ตัวนี้ต้องวางบนพื้น `--color-paper` เท่านั้น** ถ้าย้ายไปวางบนพื้นสีอื่น
(เช่น footer สีเข้ม หรือ favicon) ต้องแก้ค่า fill ตามพื้นนั้นด้วย

โลโก้นิ่งเสมอ ไม่มีแอนิเมชัน เพราะตราประทับในการ์ดผลลัพธ์เป็นจุดเดียวที่ได้สิทธิ์เคลื่อนไหว

**การเข้าถึง**: ทุกแอนิเมชันอยู่ใต้ `@media (prefers-reduced-motion: reduce)` —
คนที่ปิด motion จะเห็นตราประทับปรากฏเฉยๆ ไม่หมุน ไม่ประทับ

---

## 5. ทบทวนแผน — จุดที่ generic และสิ่งที่แก้

ทบทวนแผนข้างบนก่อนเขียนโค้ด แล้วพบว่ามี 5 จุดที่ยังเป็นของที่ "เจอได้ทุกเว็บ" จึงแก้ดังนี้

| จุดที่ generic | เดิม | แก้เป็น | เหตุผล |
|---|---|---|---|
| พื้นหลัง hero | `bg-gradient-to-b from-blue-50 to-white` | พื้นกระดาษอุ่นสีเดียว `#F6F3EC` ไม่มี gradient | gradient ฟ้าอ่อนไล่ลงขาวคือลายเซ็นของ template AI ทุกตัว และไม่ได้สื่ออะไรเลย |
| ตัวโหลด | spinner วงกลมหมุน | วงแหวนตราประทับหมุน | spinner คือ default ที่ไม่มีใครจำได้ ส่วนตราประทับต่อยอดเป็น signature ได้ |
| ป้ายสถานะ | pill สี่เหลี่ยมมนแบบ badge | ตราประทับวงกลมมีวันที่ข้างใน | badge pill คือภาษาของ dashboard ไม่ใช่ภาษาของไปรษณีย์ |
| หัวข้อ hero | "ติดตามพัสดุทุกขนส่งในที่เดียว" | **"พัสดุถึงไหนแล้ว"** | ประโยคเดิมเป็นภาษาการตลาดแบบ "all-in-one" ที่ SaaS ใช้เหมือนกันหมด ประโยคใหม่คือคำถามที่ผู้ใช้คิดอยู่จริงตอนเปิดเว็บ |
| placeholder | "กรอกเลขพัสดุของคุณ" | "เช่น EE000000000TH" | บอกรูปแบบที่ต้องกรอกจริง ดีกว่าบอกสิ่งที่ผู้ใช้รู้อยู่แล้ว |

เพิ่มอีกข้อจากการทบทวน: **ข้อความ error ต้องบอกทางแก้เสมอ** ไม่ใช่แค่บอกว่าพัง
เช่น "ไม่พบเลขนี้" ต้องตามด้วยว่าถ้าเพิ่งส่งวันนี้ ขนส่งมักใช้เวลา 1-2 ชม. ถึงจะขึ้นระบบ
เพราะนั่นคือสาเหตุที่พบบ่อยที่สุดจริงๆ ของการค้นไม่เจอ

---

## 6. กติกาที่ต้องรักษาไว้

- สีทุกสีมาจาก CSS variable ใน `app/globals.css` เท่านั้น — ห้ามเขียน hex กระจายในไฟล์ component
- Mobile-first เสมอ: เขียน class ของจอเล็กก่อน แล้วค่อยเติม `sm:` / `md:`
- ทุกอย่างที่กด/โฟกัสได้ต้องมี `:focus-visible` ที่เห็นชัด (เส้นขอบ 2px + ระยะห่าง 2px)
- ทุกแอนิเมชันต้องเคารพ `prefers-reduced-motion`
- ข้อความทุกชิ้นเป็นภาษาที่คนทั่วไปเข้าใจ ไม่มีศัพท์เทคนิค ไม่มีรหัส error ให้ผู้ใช้เห็น
